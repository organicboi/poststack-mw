import proxyController from './proxy.controller';
import { BackendProxyService, backendProxy } from './backend.proxy';

export { proxyController, BackendProxyService, backendProxy };

export default proxyController;
